{
  "name": "@kohaku/yidm-v3",
  "version": "0.0.3",
  "description": "Some key functions used by Yidm Android APP.",
  "main": "src/index.ts",
  "typings": "build/index.d.ts",
  "author": {
    "name": "Nanami"
  },
  "files": [
    "build"
  ],
  "license": "MIT",
  "scripts": {
    "start": "node --require ts-node/register src/index.ts",
    "build": "rimraf ./build && tsc -d --outDir build",
    "prepublish": "yarn build",
    "postbuild": "node script/after.js",
    "test": "node script/test.js",
    "pretest": "yarn build",
    "upload": "yarn publish --access public"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
